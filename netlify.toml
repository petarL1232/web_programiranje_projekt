[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "dist/client/browser"

[build.environment]
  NODE_VERSION = "22.16.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
