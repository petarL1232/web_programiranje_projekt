{
  "name": "document-chain-web-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace tooling for the DocumentChain Web Programming project.",
  "scripts": {
    "format": "prettier --write \"**/*.{js,ts,html,scss,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{js,ts,html,scss,json,md,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
